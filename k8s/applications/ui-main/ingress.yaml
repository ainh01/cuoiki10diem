apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ui-main
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ui-main
            port:
              number: 3001
