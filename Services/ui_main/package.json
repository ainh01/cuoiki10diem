{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "healthcheck": "node -e \"require('http').get('http://127.0.0.1:3000/health', res => { if(res.statusCode===200) { console.log('OK'); process.exit(0) } else { console.error('UNHEALTHY - status: ' + res.statusCode); process.exit(1) } })\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@types/node": "^18",
    "@types/pdfkit": "^0.12.10",
    "nuxt": "^3.6.1",
    "nuxt-icon": "^0.4.2",
    "nuxt-simple-robots": "^3.0.2"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "jwt-decode": "^4.0.0",
    "nuxt-swiper": "^1.1.1",
    "pdfkit": "^0.13.0",
    "vue-awesome-paginate": "^1.1.46",
    "vuex": "^4.0.2"
  }
}
